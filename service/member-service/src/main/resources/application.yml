spring:
  application:
    name: member-service
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/example_db?useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: example

  data:
    redis:
      host: 127.0.0.1
      port: 6379
